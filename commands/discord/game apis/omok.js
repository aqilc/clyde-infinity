import { errorm } from "../../../func/discord/f.js"

export default {
	async f ({ m, embed, content, send }) {
		const user = 1// = await ask(m, embed.t("Who are you playing with?"), "🙌")[0];
		console.log(user);
	}
		
}
/**
 # Available Export Formats:
 - after<func[]>: Events added after the client events fire.
   - "this":
     - bot<object>:
 - before<func[]>: Events added before the client events fire.
 - cmds<(string | Command)[]>: Additional commands this module includes.
 */

/**
 *
 */
export const after = {
  reaction (msg) {

  },
  raw: msg => {

  }
}
